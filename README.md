# criar o ambiente para isolar todas as bibliotecas
    python -m venv venv

# ativar
    venv/Scripts/activate

# criar o arquivo e depois para instalar dependências
    pip install -r requirements.txt